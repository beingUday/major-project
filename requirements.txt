# ============================================================
# Varta-Sync — Combined Requirements
# Agent 1 (Sign → Speech) + Agent 2 (Speech → Sign)
# Python 3.10.11
# ============================================================
# Install command:
#   pip install -r requirements.txt
#
# ── PyAudio note (Windows) ───────────────────────────────────
# If pyaudio fails during install, run:
#   pip install pipwin
#   pipwin install pyaudio
# Then re-run this file.
#
# ── GPU note (optional) ──────────────────────────────────────
# The torch below installs CPU version (fine for Agent 2).
# If your laptop has an NVIDIA GPU, replace torch install with:
#   pip install torch==2.1.2+cu118 torchaudio==2.1.2+cu118
#         --index-url https://download.pytorch.org/whl/cu118
# ============================================================


# ── Core ML (Agent 1) ────────────────────────────────────────
tensorflow==2.13.0
keras==2.13.1
scikit-learn==1.3.2

# ── Speech to Text (Agent 2) ─────────────────────────────────
torch==2.1.2
torchaudio==2.1.2

# ── Fuzzy Matching (Agent 2) ─────────────────────────────────
rapidfuzz==3.6.1

# ── Computer Vision — shared by both agents ──────────────────
mediapipe==0.10.9
opencv-python==4.8.1.78

# ── Speech Output (Agent 1) ──────────────────────────────────
pyttsx3==2.90

# ── Microphone Input (Agent 2) ───────────────────────────────
pyaudio==0.2.14
sounddevice==0.4.6
soundfile==0.12.1

# ── Shared Utilities ─────────────────────────────────────────
numpy==1.24.3
pandas==2.0.3
tqdm==4.66.1